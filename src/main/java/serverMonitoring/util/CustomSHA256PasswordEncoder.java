package serverMonitoring.util;

/**
 * Created with IntelliJ IDEA.
 * User: serge
 *
 * this class responsible for the password encoding String to SHA-256
 */
public class CustomSHA256PasswordEncoder {

//    public static String hashTo256(String data) throws NoSuchAlgorithmException {
//        MessageDigest md = MessageDigest.getInstance("SHA-256");
//        md.update(data.getBytes());
//        return bytesToHex(md.digest());
//    }
//
//    public static String bytesToHex(byte[] bytes) {
//        StringBuffer result = new StringBuffer();
//        for (byte byt : bytes) result.append(Integer.toString((byt & 0xff) + 0x100, 16).substring(1));
//        return result.toString();
//    }

}
